/*
  This script must be executed at the end of the HTML file
  
  Allows the Javascript to callback the WebView when the document is
  ready. This assumes that the WebView was given an 'android'
  JavascriptInterface with the 'documentReady()' method.
*/
android.onDocumentReady();
